{
  "name": "cp-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node_modules/.bin/gatsby --prefix-paths build",
    "deploy": "node_modules/.bin/gatsby build --prefix-paths && node_modules/gh-pages/bin/gh-pages -d public",
    "develop": "node_modules/.bin/gatsby develop",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gatsby": "^1.0.7",
    "gatsby-link": "^1.0.8",
    "gatsby-plugin-sass": "^1.0.1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0"
  }
}
